# The setup repo only stores artifacts shared across all projects.
# Per-project subfolders are added locally and are not in git.
# Whitelist: everything at the top level is ignored except for the explicit allow-list below.

/*
!/.gitignore
!/Dockerfile
!/init.sh
!/connect-project.md
!/bin
!/cc-docker-proxy
!/lib
!/README.md
